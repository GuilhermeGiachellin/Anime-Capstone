export function countAnime(data) {
  const allAnime = data.anime.length;
  return allAnime;
}

// returns number of comments
export function commentCounter(data) {
  const allComments = data.length;
  return allComments;
}